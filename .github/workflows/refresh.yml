name: Refresh dashboard
on:
  schedule:
    # hourly during US market hours, 9:30 AM - 4:30 PM Eastern (summer time)
    - cron: "0 11 * * 1-5"      # 7:00 AM ET  pre-market
    - cron: "0 12 * * 1-5"      # 8:00 AM ET  pre-market
    - cron: "0 13 * * 1-5"      # 9:00 AM ET  just before the open
    - cron: "30 13-20 * * 1-5"  # 9:30 AM - 4:30 PM ET  hourly
    - cron: "0 21 * * 1-5"      # 5:00 PM ET  after close
    - cron: "0 23 * * 1-5"      # 7:00 PM ET  evening
  workflow_dispatch: {}

permissions:
  contents: write

# only one refresh at a time - overlapping runs caused racing Pages deploys
# that cancelled each other and left the live site stale
concurrency:
  group: dashboard-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install --quiet pandas numpy ephem esprima
      - run: python fetch_data.py
      - run: python backfill.py
      - run: python benchmark.py
      - run: python analyze.py
      - run: python coherence_check.py
      - run: python anomaly_audit.py
      - name: Commit and push updated dashboard
        run: |
          git config user.name "dashboard-bot"
          git config user.email "dashboard-bot@users.noreply.github.com"
          git add index.html dashboard_single.html data.js predictions_log.json daily_extremes.json trades.json trades_log.json horizons_log.json real_trades.json improvements_log.md backfill.json benchmark.json week_plan.json week_plan_log.json
          git commit -m "auto refresh (cloud)" || echo "nothing new"
          git push

